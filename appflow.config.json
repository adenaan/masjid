{
  "apps": [
    {
      "appId": "39837a21",
      "dependencyInstallCommand": "node -v && npm -v && npm i -g npm@10 && npm -v && npm install --foreground-scripts --no-audit --no-fund",
      "webBuildCommand": "npm run build"
    }
  ]
}
