{
  "apps": [
    {
      "appId": "39837a21",
      "dependencyInstallCommand": "rm -rf node_modules package-lock.json && npm install --include=optional --no-audit --no-fund",
      "webBuildCommand": "npm run build"
    }
  ]
}
